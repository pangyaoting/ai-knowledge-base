/**
 * 演示 / 审核体验账号（公安联网备案审核员、面试官用）。
 *
 * 为什么放在代码里：登录页要显示账号密码并支持"一键体验"，必须前端可见（公开信息）。
 * 安全边界靠三件事，缺一不可：
 *   1. **不绑任何模型配置** → 对话/研究报告/自主研究全部不可用，花不到大模型的钱（BYO 架构天然如此）
 *   2. **服务端只读守卫** → 该邮箱的账号所有写操作 403（上传文档会用系统 Key 做向量化，必须挡住）
 *      服务端要配 `DEMO_READONLY_EMAILS=zhizhuojuzi.h@qq.com`，否则这张卡片就只是个普通账号
 *   3. 里面只有**演示数据**（预置知识库/对话/报告），看不到主账号的任何内容
 *
 * 设为 `null` 即在登录页隐藏这张卡片（比如以后不想公开账号时）。
 */
export const DEMO_ACCOUNT: { email: string; password: string; note: string } | null = {
  email: 'zhizhuojuzi.h@qq.com',
  password: 'Demo@2026',
  note: '只读演示：可浏览知识库、对话、研究报告与数据看板；写操作与 AI 功能已关闭',
};
