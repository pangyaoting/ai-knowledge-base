# 41 · 三个月深度学习与求职冲刺计划（可执行版）

> 目标：2026-09 中 ~ 2026-12 底，12 周把 JS/TS/Vue 地基打牢 + 把本项目吃到"能讲能写能改"，
> 2027-01 开始求职。这份笔记是**教练手册**：每天打开，照做，做完打勾，没过关不进入下一天。

---

## Part 0 · 怎么用这份计划

### 铁律（违反 = 计划无效）
1. **每天必须产出**：代码 ≥ 20 行 或 笔记 ≥ 200 字，不许只"看"。
2. **自测不过关不前进**：每章末尾的"自测题"闭卷能写/能讲，才算过。
3. **每天碰项目 ≥ 1 小时**：哪怕只读一个文件、给一处代码写注释。
4. **每周日晚填打卡表**（Part 7），发我复盘。

### 每日时间模板（在校版，一周 ≈ 28h）
| 时段 | 内容 |
|---|---|
| 工作日 13:00-15:00 | 深学块（当天主题理论+练习） |
| 工作日 15:30-17:30 | 项目块（读码/复写/二开） |
| 工作日 20:00-21:30 | 复习块（补练习/刷题/写笔记） |
| 周末上午 | 本周大任务（算法/二开功能，连续 3h） |
| 周末下午 | 项目深读（对照 docs） |
| 周末晚上 | 打卡表复盘 + 下周计划 |

### 面试导向（学每个知识前问自己：面试官会怎么考？）
| 知识 | 面试怎么考 |
|---|---|
| JS 闭包/this/原型 | 手写题 + 输出题："这段代码输出什么，为什么" |
| 异步/事件循环 | "宏任务微任务执行顺序""为什么用 rAF 节流" |
| TS | 给场景写类型 / 泛型约束 |
| Vue3 | 响应式原理手写 / 组件通信 / 为什么 KeepAlive |
| 网络 | HTTP 缓存 / SSE vs WebSocket / 输入 URL 到页面过程 |
| 项目 | 一条链路讲 5 分钟 + 深挖"为什么这么设计" |

---

## Part 1 · 12 周总路线图

| 阶段 | 周 | 主题 | 过关标准（不过不前进） |
|---|---|---|---|
| 一 语言重修 | W1 | JS 核心上 | 闭卷手写 bind/防抖/深拷贝 |
| | W2 | JS 核心下 | 讲清事件循环，画出本项目流式的异步时序 |
| | W3 | TypeScript | 独立给接口写类型；说清三层类型关系 |
| | W4 | 浏览器/网络+全景 | 对着模块地图讲 5 分钟架构 |
| 二 框架+项目 | W5 | Vue3 响应式 | 手写 50 行 reactive/effect |
| | W6 | Vue3 渲染+三链路 | 录音讲清三条核心链路 |
| | W7 | 二开功能 1 | 完成 1 个新功能全流程+commit |
| | W8 | 二开功能 2-3 | 至少 1 个前后端联动功能 commit |
| 三 求职冲刺 | W9 | 算法专题 | 累计 40-50 题，每题能讲思路 |
| | W10 | 八股+讲解稿 | 3 分钟 STAR 自我介绍 + 项目 20 问 |
| | W11 | 简历+模拟面 | 简历定稿 + 5 场模拟面试 |
| | W12 | 投递+复盘 | 投 50+，每场 2h 内复盘 |

---

## Part 2 · 阶段一：语言重修（W1-4）

### W1 JavaScript 核心（上）——闭包/作用域/原型/this

**资源**：现代 JavaScript 教程 zh.javascript.info（免费中文）：「对象：基础知识」→「原型继承」→「变量作用域，闭包」→「对象方法，this」→「函数进阶」。

| 天 | 学习内容 | 练习（产出） |
|---|---|---|
| 周一 | 变量作用域与闭包（词法环境、闭包定义与用途） | 手写 3 个闭包 demo：计数器/防抖/一次性函数 |
| 周二 | 闭包经典题 + 垃圾回收与闭包内存 | 闭卷解释 3 道闭包输出题（写答案+原因） |
| 周三 | 对象与 this（四种调用方式、箭头函数 this） | 手写 call/apply/bind（bind 必须闭卷） |
| 周四 | 原型与原型链（__proto__/prototype、继承） | 手画原型链图；实现一个 class 继承并讲清 new 过程 |
| 周五 | 深拷贝/浅拷贝 + 类型判断（typeof/instanceof） | 闭卷手写深拷贝（考虑数组/循环引用） |
| 周六 | 本周错题重做 + 总自测 | 自测题闭卷全做（见下） |
| 周日 | 项目结合：在代码里找证据 | 读 `apps/web/src/composables/useToast.ts` 与 `utils/cn.ts`，标注用到的概念，写 10 行注释 |

**W1 自测题（闭卷）**：① 什么是闭包？闭包解决了什么问题？② 手写 bind。③ 手写防抖。④ `const obj={a:1}; const f=()=>this.a` 与普通函数 this 区别。⑤ new 一个函数时内部发生什么？

### W2 JavaScript 核心（下）——异步/事件循环/模块

**资源**：zh.javascript.info：「Promise」→「async/await」→「事件循环：微任务和宏任务」→「模块」→「Proxy 与 Reflect」（了解）。

| 天 | 学习内容 | 练习（产出） |
|---|---|---|
| 周一 | Promise 基础（状态/链式/错误处理） | 手写 Promise.all / Promise.race（闭卷） |
| 周二 | async/await + 错误捕获 | 改写一段回调为 async；手写"串行 vs 并行"请求 |
| 周三 | 事件循环（宏任务/微任务/渲染时机） | 写 10 道"输出顺序"题并自答 |
| 周四 | 模块化（ESM/CJS） + 项目为什么用 ESM | 读项目 `package.json`，说明 scripts/依赖管理 |
| 周五 | 节流/防抖 + rAF 原理 | 手写 throttle；查 rAF 与 setTimeout 区别 |
| 周六 | 总自测 + 错题重做 | 自测题闭卷 |
| 周日 | **项目结合（本周重点）** | 读 `apps/web/src/views/Chat.vue` 的流式接收逻辑，**画出"消息到达→状态更新→渲染"的异步时序图**，标出哪些是微任务/宏任务/渲染帧 |

**W2 自测题**：① `setTimeout(0)` vs `Promise.then` 谁先执行，为什么？② 手写 Promise.all。③ 为什么流式输出用 rAF 节流而不是每帧 setState？④ async 函数返回什么？⑤ 微任务队列在什么时候被清空？

> 💡 W2 的时序图是面试**核武器**：能讲清"SSE 数据流 + rAF 节流 + 不卡 UI"的学生极少。

### W3 TypeScript 系统化

**资源**：TypeScript 官方手册中文（基础类型→接口→类→泛型→类型收窄→工具类型 Partial/Pick/Omit/Record）；项目即最佳练习场。

| 天 | 学习内容 | 练习（产出） |
|---|---|---|
| 周一 | 基础类型/接口/类型别名/字面量类型 | 给项目 3 个 API 响应写类型 |
| 周二 | 泛型（函数/接口/约束） | 手写 `function identity<T>` 及泛型约束场景 |
| 周三 | 类型收窄（typeof/in/判别联合）+ 工具类型 | 手写 5 个工具类型的使用场景 |
| 周四 | class + 装饰器概念（NestJS 用） | 读 `apps/server/src/modules/chat/dto/ask.dto.ts`，讲清 class-validator 装饰器作用 |
| 周五 | 项目类型全景 | 读 `apps/web/src/types/model-config.ts`、`types/chat.ts`、`packages/shared` 类型，画"前后端类型如何对应" |
| 周六 | 总自测 | 自测题闭卷 |
| 周日 | 写一篇"TS 帮我抓到的 bug"笔记 | 翻项目 git log，找 1 个类型相关修复，写成 200 字复盘 |

**W3 自测题**：① 泛型和 any 区别？② `Partial<T>` 干什么？③ 判别联合怎么用？④ class-validator 的 DTO 校验发生在哪一层（对照代码讲）？⑤ 项目里 `string | null` 与 `string?` 区别？

### W4 浏览器/网络原理 + 项目全景

**资源**：MDN（HTTP 缓存、SSE）；对照项目真实配置学习。

| 天 | 学习内容 | 练习（产出） |
|---|---|---|
| 周一 | 输入 URL 到页面渲染全过程 | 画流程图（DNS→TCP→HTTP→解析→渲染） |
| 周二 | HTTP 缓存（强缓存/协商缓存）+ 项目实践 | 读服务器 nginx 配置（/assets 30d、/avatars no-store），讲清为什么这么配 |
| 周三 | SSE vs WebSocket vs 轮询 | 读 `apps/server/src/modules/chat/chat.controller.ts` 的 SSE 接口与前端接收处，写对比笔记 |
| 周四 | 浏览器渲染（DOM/CSSOM/回流重绘/合成） | 列出本项目 3 处可优化点（如图片懒加载） |
| 周五 | 通读项目 docs：00→02→09/10→13 | 写 300 字"项目为什么这么设计" |
| 周六 | **画模块地图（里程碑产出）** | 画图：页面→组件→store/api→server 模块→数据库表，标出数据流方向 |
| 周日 | 对着地图讲 5 分钟（录音） | 回听，补讲不清的地方；地图存进 docs/ 供后续更新 |

**W4 过关**：不看代码，能对着模块地图说出：① 有哪些页面与核心组件；② 一条消息从发送到回答渲染经过哪些层；③ 数据库有哪些表、各自作用。

---

## Part 3 · 阶段二：框架 + 项目吃透（W5-8）

### W5 Vue3 响应式原理

**资源**：Vue3 官方文档（v3.cn.vuejs.org）：「响应式基础」→「深入响应式系统」→「组合式 API」。

| 天 | 学习内容 | 练习（产出） |
|---|---|---|
| 周一 | ref/reactive 区别与使用 | 把项目 1 处 `ref` 改 `reactive`（或反之）并说明差异 |
| 周二 | computed/watch/watchEffect | 找项目 3 处 computed 的用法（如 `ChatMessageItem.vue` 的 imgList/msgHtml），讲清为什么用 computed |
| 周三 | 响应式原理（effect/dep/依赖收集） | **手写迷你 reactive+effect（50 行内，闭卷）** |
| 周四 | 组合式 API vs 选项式 + 生命周期 | 对比项目组件用的写法，画组件生命周期图 |
| 周五 | 组件通信（props/emit/provide-inject/store） | 梳理 Chat.vue 与子组件（ChatMessageItem 等）的通信方式，画关系图 |
| 周六 | 深入：为什么 KeepAlive 要用 onActivated | 读 `apps/web/src/views/Layout.vue` 与 `views/Chat.vue`，讲清"缓存组件如何刷新数据" |
| 周日 | 本周总结 | 写"Vue3 响应式 500 字笔记"：从 ref 到页面更新发生了什么 |

**W5 自测题**：① ref 和 reactive 区别？② 手写 reactive/effect 思路。③ 为什么 `msg.id` 作 key 而不是 index（项目改过，翻 git log 找答案）？④ KeepAlive 组件 onActivated 何时触发？⑤ computed 为什么能缓存？

### W6 Vue3 渲染机制 + 三条核心链路精读

**资源**：Vue3 官方文档「渲染机制」「渲染函数」；本项目源码精读。

| 天 | 学习内容 | 练习（产出） |
|---|---|---|
| 周一 | 虚拟 DOM / diff / 更新流程 | 讲清"数据变了页面怎么更新" |
| 周二 | **链路一：文档上传→RAG 问答** | 精读：`knowledge/document-queue.service.ts`（队列）→ `utils/document-parser.ts`（解析）→ `utils/text-splitter.ts`（切片）→ `embedding.service.ts`（向量化）→ 存储 → `chat/rag.service.ts`（检索）→ `chat/rrf.ts`（融合排序）。每文件写 5 行注释 |
| 周三 | 链路一续：向量检索原理 | 读 prisma schema 的 vector 字段与迁移 SQL；查 pgvector 相似度检索原理，写笔记 |
| 周四 | **链路二：SSE 流式聊天** | 精读：`chat/chat.service.ts`（生成/流式）→ `chat.controller.ts`（SSE 端点）→ `apps/web/src/views/Chat.vue` 接收处（rAF 节流）→ `utils/markdown.ts`（渲染缓存）。画端到端时序图 |
| 周五 | **链路三：认证 + BYO 模型配置** | 精读：`auth/email-code.service.ts`（验证码）→ `auth/auth.service.ts`（JWT）→ `strategies/jwt.strategy.ts`（守卫）→ `models/model-config.service.ts`（多模型）→ `apps/web/src/components/ModelConfigForm.vue`。画登录到提问全流程 |
| 周六 | 录音自测三条链路 | 每条链路对着代码讲 5 分钟，录下来回听 |
| 周日 | 复盘 + 补漏 | 把讲不清的地方重新精读，写"三条链路"总结笔记 |

**W6 过关**：三条链路每条能对着代码讲 5 分钟，能答"为什么用队列？为什么切片？RRF 解决什么？为什么 SSE 关缓冲？"

### W7 项目二开（上）——第一个自研功能

**选功能**（从易到难，任选）：
- A. 会话导出 Markdown（前端为主）
- B. 文档树拖拽整理（前端 + 接口）
- C. 引用溯源面板增强（前后端）

| 天 | 学习内容 | 练习（产出） |
|---|---|---|
| 周一 | 定方案：选功能，写方案对比（2 种做法利弊） | 方案文档 |
| 周二 | 读相关现有代码，画改动点清单 | 改动清单（文件×改动） |
| 周三-四 | 实现（前端或后端先通一边） | 代码 commit |
| 周五 | 补另一边 + 自测（正常/边界/报错） | 自测记录 |
| 周六 | 写 docs 记录 + 单测（如有） | docs 章节 |
| 周日 | 全流程 review：能不能讲清每行 | 录音讲实现 |

**W7 过关**：1 个功能完整走完"方案→实现→自测→docs→commit"，面试能讲"我做了什么、为什么这么做、遇到什么坑"。

### W8 项目二开（下）——前后端联动功能 + SQL

| 天 | 学习内容 | 练习（产出） |
|---|---|---|
| 周一 | SQL 基础：增删改查/联表/聚合 | 用 pgAdmin/psql 对本地库跑 5 条查询 |
| 周二 | Prisma：schema 改动→迁移→seed | 读 `apps/server/prisma/schema.prisma` 与一个迁移 SQL，自己加 1 个字段走完整流程（参考 docs 里 model_configs 迁移记录） |
| 周三-五 | 二开功能 2（必须改 schema 或加接口） | 全流程 + commit |
| 周六 | 二开功能 3（修复一个已知小问题或加小优化） | commit |
| 周日 | 整理 3 个功能的 docs + 复盘 | docs 章节 + 打卡 |

**W8 过关**：2-3 个可写进简历的功能 commit；能讲清"改一个字段从前端到数据库要动哪几层"。

---

## Part 4 · 阶段三：求职冲刺（W9-12）

### W9 算法专题（每天 1-2 题，JS）

**专题顺序**：数组→哈希→双指针→字符串→栈队列→链表→二叉树→动态规划入门（简单）。
**资源**：LeetCode 热题 100（简单+中等）；想不出 30 分钟看题解，**合上重写一遍**。
**每日格式**：题目 → 思路（2 句）→ 代码 → 复杂度。

| 天 | 主题 | 目标题量 |
|---|---|---|
| 周一 | 数组+哈希（两数之和/有效的字母异位词等） | 2 |
| 周二 | 双指针（移动零/盛最多水的容器等） | 2 |
| 周三 | 字符串+栈（有效的括号/反转字符串等） | 2 |
| 周四 | 链表（反转链表/环形链表等） | 2 |
| 周五 | 二叉树（前中后序/最大深度等） | 2 |
| 周六 | DP 入门（爬楼梯/打家劫舍等） | 2 |
| 周日 | 本周错题重做 + 总结 | 复习 |

**W9 过关**：累计 40-50 题；每个专题至少 1 题能讲思路+复杂度。

### W10 八股 + 项目讲解稿

| 天 | 内容 | 产出 |
|---|---|---|
| 周一 | JS/TS 八股复习（W1-3 自测题重做） | 错题笔记 |
| 周二 | Vue3 八股（响应式/生命周期/通信/KeepAlive） | 每题 2 句答案卡 |
| 周三 | 浏览器/网络八股（缓存/SSE/输入 URL 过程/跨域） | 答案卡 |
| 周四 | 工程化八股（Vite/webpack/pnpm monorepo/CI-CD/部署） | 答案卡（结合 docs/39 真实经历） |
| 周五 | **项目 20 问 Q&A 撰写**（见 Part 6 题库） | 每题 3-5 句答案 |
| 周六 | **3 分钟 STAR 自我介绍稿**（复用 docs/37） | 定稿背诵 |
| 周日 | 全流程模拟：自我介绍+项目 20 问抽查 | 录音回听 |

### W11 简历 + 模拟面试

| 天 | 内容 |
|---|---|
| 周一 | 按 docs/37 改简历（STAR 化 3 段项目经历），发我审 |
| 周二 | 投递渠道搭建：BOSS直聘/牛客/实习僧完善档案，写打招呼模板 |
| 周三-六 | 每天 1 场模拟面试（我出题）：JS 手写轮 / Vue 原理轮 / 项目深挖轮 / 综合轮；每天投 5-10 家 |
| 周日 | 复盘本周面试问题，汇总"被问倒清单"逐条补 |

### W12 投递实战 + 复盘

| 天 | 内容 |
|---|---|
| 周一-五 | 每天投 5-10 家 + 复盘已投岗位要求，微调简历关键词 |
| 周六 | 已拿到面试：模拟该司可能问题；没拿到：复盘简历/渠道 |
| 周日 | 目标核对：投 50+、3+ 面试；制定下周迭代计划 |

---

## Part 5 · 吃透项目四步走（随时回来对照）

1. **通读 docs**（顺序）：00 → 02 → 06/08 → 09/10 → 11 → 13 → 23 → 29 → 36 → 39
2. **模块地图**：W4 产出后每周更新（页面/组件/store/api/server/DB 与数据流）
3. **三链路精读**（W6）：每步写注释、问为什么
4. **复写 + 二开**（W7-8）：先关书实现小函数（text-splitter / rrf 融合 / 竞态防护），再上整功能

> 心法：**不要"读代码"，要"改代码"**。git log 是最好的教科书——本项目每个坑都有对应 commit（图片 JSON bug、竞态、KeepAlive 刷新、BOM 迁移），翻出来看"当时为什么改"。

---

## Part 6 · 面试题库（背熟）

### 项目 20 问（每题准备 3-5 句答案）
1. 项目整体架构？（一句话 + 模块地图）
2. 为什么用 pgvector 做向量检索？
3. 文档上传后发生了什么？（队列→解析→切片→向量化）
4. 文本切片为什么这么切？chunk 大小怎么定的？
5. RRF 是什么？为什么用它融合检索结果？
6. SSE 怎么实现的？和轮询/WebSocket 区别？
7. 流式输出为什么用 rAF 节流？怎么防止 UI 卡顿？
8. 图片 JSON 字符串 bug 是怎么发现和修复的？
9. 会话切换的竞态问题怎么防？（selectSession 竞态）
10. 为什么列表 key 用 msg.id 不用 index？
11. KeepAlive 缓存了 ChatView，怎么刷新模型列表？
12. 邮箱验证码怎么做的？为什么不用短信？
13. JWT 认证流程？token 放哪？过期怎么办？
14. BYO 模型配置怎么设计？一配置多模型怎么选？
15. 上传速度/性能优化做了哪些？（部署后为什么变快）
16. CI/CD 怎么搭的？push 即上线怎么实现？
17. 数据库有哪些表？会话和文档关系？
18. 用过哪些队列任务？BullMQ + Redis 解决什么？
19. 项目测试怎么写的？37 个单测覆盖什么？
20. 如果用户问的问题知识库里没有，怎么兜底？（门控+无资料回答）

### 八股速查（每题背 2 句）
- JS：闭包/原型链/this/事件循环/Promise/防抖节流/深拷贝
- TS：泛型/工具类型/类型收窄
- Vue：响应式原理/computed 缓存/生命周期/组件通信/KeepAlive/diff
- 网络：HTTP 缓存/SSE/跨域/输入 URL 全过程
- 工程：Vite 热更新原理/pnpm monorepo/CI 流程/nginx 反代

---

## Part 7 · 打卡表（每周日晚填，发我复盘）

| 周次 | 本周产出（代码 commit / 笔记 / 题数） | 卡点 | 下周关键动作 |
|---|---|---|---|
| W1 | | | |
| W2 | | | |
| W3 | | | |
| W4 | 模块地图 | | |
| W5 | | | |
| W6 | 三链路录音 | | |
| W7 | 功能 1 commit | | |
| W8 | 功能 2-3 commit | | |
| W9 | 算法 40+ | | |
| W10 | 讲解稿 + 20 问 | | |
| W11 | 简历 + 5 场模拟 | | |
| W12 | 投 50+ | | |

---

## 附 · 求职策略与转型预案

- **投递定位**：前端开发 / AI 应用开发 / 全栈；中小厂为主，外包/实习转正保底
- **简历主卖点**：全栈 AI 知识库（RAG+Agent+流式+CI/CD）+ 37 单测 + 40 篇 docs 工程习惯
- **时间线**：若 2028 毕业（本科）→ 2027.1 起投暑期实习（3-5 月春招季、7-10 月秋招）；若 2027 毕业 → 直接冲正式批
- **转型预案**（仅当 W8 写不出功能 / W10 刷不动题再启动）：A 加 Node 深度转全栈（首选）；B 加 Python+LLM 框架投 AI 应用开发；C 前端+UI/UX 设计感。❌ 别碰算法/AI 算法岗
- **市场背景**（2026-09 调研）：前端初级岗收缩、AI 应用岗增长；本项目正是市场要的"前端+AI 应用"复合证明，别浪费
